export const PLUGIN_ID = '@imgly/plugin-ai-video-generation-web';

/**
 * Default order for video quick actions.
 * This is used as a fallback when children is undefined in canvas menu order.
 */
export const DEFAULT_VIDEO_QUICK_ACTION_ORDER = [
  'ly.img.createVideo',
  'ly.img.animateBetweenImages'
];
