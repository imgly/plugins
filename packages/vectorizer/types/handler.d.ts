import { CreativeEngine } from "@cesdk/cesdk-js";
/**
 * Handle every possible state of the vectorization state if the block was
 * updated.
 */
export declare function update(engine: CreativeEngine, blockId: number): Promise<void>;
